#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout << "Hello "; // 这句话会输出
    cout.flush();     // 强制刷新输出
    cin.get();        // 等待输入
    cout << "World!" << endl;
    return 0;
}
