#include <iostream>
using namespace std;
#include <list>
struct TreeNode
{
    int val;
    struct TreeNode *left;  // 左子树
    struct TreeNode *right; // 右子树
    TreeNode(int x) : val(x), left(NULL), right(NULL)
    {
    }
};
TreeNode *theLeastLeftNode
{
}
TreeNode *Convert(TreeNode *pRootOfTree)
{
}